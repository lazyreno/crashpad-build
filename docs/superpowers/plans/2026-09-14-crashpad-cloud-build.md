# Crashpad Cloud Build Implementation Plan

Plan: declaration-driven GitHub Actions builds for macOS arm64/x86_64 and Windows arm64/x86_64, immutable GitHub Releases, CMake package, and audiocut integration. Detailed task breakdown is in the task response.
